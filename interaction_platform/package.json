{
  "name": "@ai-bot/interaction-platform",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "seed": "bun run src/seed.ts",
    "test": "bun test",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ai-bot/shared-db": "workspace:*",
    "drizzle-orm": "^0.43.1",
    "hono": "^4.7.5"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "bun-types": "^1.2.8",
    "drizzle-kit": "^0.31.1",
    "typescript": "^5.9.3"
  }
}
