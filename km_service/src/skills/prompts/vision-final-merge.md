以下是一张大型流程图的总览和各区域的合并结果。请输出完整的流程图。

## 总览（全局结构和阅读顺序）
{{OVERVIEW}}

## 各区域合并结果
{{PARTS_SECTION}}

## 输出要求

1. **流程描述**：{{DESC_LANG}}，完整的流程自然语言描述。

2. **Mermaid flowchart**：完整的 Mermaid flowchart TD 格式，要求：
   - {{NODE_RULE}}
   - **必须保留所有节点**，不要省略或简化——参考图有约 80 个节点，你的输出也应接近这个数量
   - 每个判断节点用 { } 菱形表示
   - 用 subgraph 对相关节点分组（如"固网总流程""移动预付费""余额查询"等）
   - 包含所有分支和判断条件
   - 如果某些连接不确定，用 %%注释 标注

## 重要
- 不要摘要、不要省略节点。原图中识别到的每一个框、每一个判断都必须出现在 Mermaid 中
- 重叠区域的重复节点去重为一个
- 按总览确定的阅读顺序（从上到下、从左到右）排列
