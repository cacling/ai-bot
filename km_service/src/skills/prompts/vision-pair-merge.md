以下是一张大型流程图中相邻区域的识别结果。请合并为一个完整的局部流程。

{{TILES_SECTION}}

## 要求
- {{DESC_LANG}}
- **保留所有识别到的节点**，不要省略或合并相似节点
- 去重：重叠区域出现的同一节点只保留一个
- 接上跨切片的箭头连接
- 输出格式：**仅输出**节点列表 + 连接关系（A → B），便于后续全局合并
- **严格控制篇幅**：每个节点只用一行描述（ID + 标签文字），不要展开解释。总输出不超过 3000 字
