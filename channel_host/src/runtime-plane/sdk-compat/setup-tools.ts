/**
 * openclaw/plugin-sdk/setup-tools compatibility
 */
export function formatCliCommand(cmd: string): string { return cmd; }
export function formatDocsLink(path: string): string { return `https://docs.openclaw.ai${path}`; }
